name: PR 53 note heading verification

on:
  pull_request:
    types: [opened, reopened, synchronize]
    branches: [main]

permissions:
  contents: write

jobs:
  verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Format affected files
        run: >-
          npx prettier --write
          src/shared/contracts/notes.ts
          src/shared/validation/notes.ts
          src/shared/validation/notes.test.ts
          src/renderer/src/modules/study/components/StudyBlockEditor.notes.test.tsx
          src/renderer/src/modules/study/components/StudyBlockEditor.insert-menu.test.tsx

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Run affected tests
        run: >-
          npx vitest run
          src/shared/validation/notes.test.ts
          src/renderer/src/modules/study/components/StudyBlockEditor.notes.test.tsx
          src/renderer/src/modules/study/components/StudyBlockEditor.insert-menu.test.tsx

      - name: Remove temporary verification files
        run: |
          rm .github/workflows/pr53-note-heading-verify.yml
          rm .github/pr53-note-heading-trigger.txt

      - name: Commit verified changes and cleanup
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
          else
            git commit -m 'feat: allow heading blocks in notes'
            git push origin HEAD:${{ github.head_ref }}
          fi
