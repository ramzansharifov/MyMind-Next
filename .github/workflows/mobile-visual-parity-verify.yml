name: Temporary mobile visual parity verify

on:
  push:
    branches:
      - fix/mobile-visual-parity-round2
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: mobile-visual-parity-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Rebuild SQLite for Node
        run: npm run native:node
      - name: Typecheck mobile
        run: npm run typecheck:mobile
      - name: Mobile tests
        run: npm run test:mobile
      - name: Expo DOM patch regression
        run: npm run test:expo-dom-patch --workspace=mymind-mobile
      - name: Mobile DOM asset regression
        run: npm run test:dom-assets --workspace=mymind-mobile
      - name: Android export
        run: npm run export:android
