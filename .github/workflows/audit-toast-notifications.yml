name: Audit toast notification patterns

on:
  push:
    branches:
      - agent/global-toastify-notifications
    paths:
      - .github/workflows/audit-toast-notifications.yml

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/global-toastify-notifications
      - name: Audit renderer feedback patterns
        shell: bash
        run: |
          echo '=== setError / error state ==='
          grep -RInE --include='*.ts' --include='*.tsx' 'setError\(|error[[:space:]]*&&|role="alert"' src/renderer/src || true
          echo '=== success / copied / saved ==='
          grep -RInE --include='*.ts' --include='*.tsx' 'setSuccess|copiedLabel|скопирован|успеш|сохранен|сохранён|создан|удален|удалён' src/renderer/src || true
          echo '=== catches in modules ==='
          grep -RInE --include='*.ts' --include='*.tsx' 'catch \(' src/renderer/src/modules || true
          echo '=== package refs ==='
          grep -RInE 'toast|Toastify|react-toastify' package.json package-lock.json src/renderer/src || true
