name: CI paused for boards integration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Boards integration is being finalized on a protected draft branch."
