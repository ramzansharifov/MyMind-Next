name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Capture latest quote test output
        shell: bash
        run: npm run test:run -- src/renderer/src/modules/study/components/rich-text/RichTextSettings.test.tsx > quote-test-latest.log 2>&1 || true
      - uses: actions/upload-artifact@v4
        with:
          name: quote-test-latest-log
          path: quote-test-latest.log
          if-no-files-found: error
