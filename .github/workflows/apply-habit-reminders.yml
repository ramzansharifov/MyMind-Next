name: Apply habit reminders

on:
  push:
    branches:
      - agent/habit-reminders

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/habit-reminders
          fetch-depth: 0

      - name: Apply habit reminders implementation
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def read(path: str) -> str:
              return Path(path).read_text(encoding='utf-8')

          def write(path: str, content: str) -> None:
              Path(path).write_text(content, encoding='utf-8')

          def replace_once(content: str, old: str, new: str, label: str) -> str:
              count = content.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected exactly one match, found {count}')
              return content.replace(old, new, 1)

          # Shared contract: persist whether fixed 30-minute reminders are enabled.
          path = 'src/shared/contracts/habits.ts'
          text = read(path)
          text = replace_once(
              text,
              "  preferredTimes: HabitPreferredTime[]\n  createdAt: number",
              "  preferredTimes: HabitPreferredTime[]\n  remindersEnabled: boolean\n  createdAt: number",
              'HabitRecord remindersEnabled',
          )
          text = replace_once(
              text,
              "  repeatEveryDays: number\n  preferredTimes: HabitPreferredTime[]\n}",
              "  repeatEveryDays: number\n  preferredTimes: HabitPreferredTime[]\n  remindersEnabled: boolean\n}",
              'CreateHabitInput remindersEnabled',
          )
          write(path, text)

          # Validation: reminders are legal only when at least one preferred time exists.
          path = 'src/shared/validation/habits.ts'
          text = read(path)
          text = replace_once(
              text,
              "    preferredTimes: z.array(habitPreferredTimeSchema).max(MAX_PREFERRED_TIMES)\n",
              "    preferredTimes: z.array(habitPreferredTimeSchema).max(MAX_PREFERRED_TIMES),\n    remindersEnabled: z.boolean()\n",
              'habit validation boolean',
          )
          text = replace_once(
              text,
              "    seenUnits.add(preferredTime.unit)\n  }\n}",
              "    seenUnits.add(preferredTime.unit)\n  }\n\n  if (input.remindersEnabled && input.preferredTimes.length === 0) {\n    context.addIssue({\n      code: 'custom',\n      path: ['remindersEnabled'],\n      message: 'Чтобы включить напоминания, укажите предпочтительное время'\n    })\n  }\n}",
              'habit reminder validation',
          )
          write(path, text)

          # Database schema: flag on habit + delivery table for de-duplication.
          path = 'src/main/database/schema/habits.ts'
          text = read(path)
          text = replace_once(
              text,
              "    preferredTime: text('preferred_time'),\n    createdAt:",
              "    preferredTime: text('preferred_time'),\n    remindersEnabled: integer('reminders_enabled', { mode: 'boolean' }).notNull().default(false),\n    createdAt:",
              'habits schema remindersEnabled',
          )
          delivery_table = """

export const habitReminderDeliveries = sqliteTable(
  'habit_reminder_deliveries',
  {
    id: text('id').primaryKey(),
    habitId: text('habit_id')
      .notNull()
      .references(() => habits.id, { onDelete: 'cascade' }),
    occurrenceDate: text('occurrence_date').notNull(),
    unit: integer('unit').notNull(),
    preferredTime: text('preferred_time').notNull(),
    deliveredAt: integer('delivered_at', { mode: 'timestamp_ms' }).notNull()
  },
  (table) => [
    uniqueIndex('habit_reminder_deliveries_habit_date_unit_unique').on(
      table.habitId,
      table.occurrenceDate,
      table.unit
    ),
    index('habit_reminder_deliveries_delivered_idx').on(table.deliveredAt)
  ]
)
"""
          if 'habitReminderDeliveries' in text:
              raise SystemExit('habit reminder delivery table already exists unexpectedly')
          text = text.rstrip() + delivery_table
          write(path, text)

          path = 'src/main/database/schema/index.ts'
          text = read(path)
          text = replace_once(
              text,
              "export { habitEntries, habitGroups, habits } from './habits'",
              "export { habitEntries, habitGroups, habitReminderDeliveries, habits } from './habits'",
              'schema index habit reminder export',
          )
          write(path, text)

          # Repository: map flag, persist it, derive due reminders from preferred times and progress.
          path = 'src/main/repositories/habits.repository.ts'
          text = read(path)
          text = replace_once(
              text,
              "  preferred_time: string | null\n  created_at: number",
              "  preferred_time: string | null\n  reminders_enabled: number\n  created_at: number",
              'HabitRow reminders_enabled',
          )
          text = replace_once(
              text,
              "  preferred_time,\n  created_at,",
              "  preferred_time,\n  reminders_enabled,\n  created_at,",
              'HABIT_SELECT reminders_enabled',
          )
          text = replace_once(
              text,
              "    preferredTimes: parsePreferredTimes(row.preferred_time),\n    createdAt:",
              "    preferredTimes: parsePreferredTimes(row.preferred_time),\n    remindersEnabled: row.reminders_enabled === 1,\n    createdAt:",
              'mapHabit remindersEnabled',
          )
          text = replace_once(
              text,
              "        repeat_every_days, preferred_time, created_at, updated_at\n      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`",
              "        repeat_every_days, preferred_time, reminders_enabled, created_at, updated_at\n      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`",
              'create habit SQL columns',
          )
          text = replace_once(
              text,
              "      serializePreferredTimes(input.preferredTimes),\n      now,\n      now",
              "      serializePreferredTimes(input.preferredTimes),\n      input.remindersEnabled && input.preferredTimes.length > 0 ? 1 : 0,\n      now,\n      now",
              'create habit reminders value',
          )
          text = replace_once(
              text,
              "        repeat_every_days = ?, preferred_time = ?, updated_at = ?\n       WHERE id = ?`",
              "        repeat_every_days = ?, preferred_time = ?, reminders_enabled = ?, updated_at = ?\n       WHERE id = ?`",
              'update habit SQL columns',
          )
          update_values_old = """      input.repeatEveryDays,
      serializePreferredTimes(input.preferredTimes),
      now,
      input.id
"""
          update_values_new = """      input.repeatEveryDays,
      serializePreferredTimes(input.preferredTimes),
      input.remindersEnabled && input.preferredTimes.length > 0 ? 1 : 0,
      now,
      input.id
"""
          text = replace_once(text, update_values_old, update_values_new, 'update habit reminders value')

          reminder_logic_anchor = """export function isHabitScheduledOn(habit: HabitRecord, date: string): boolean {
  const anchor = habitCreatedDateKey(habit)
  if (date < anchor) return false
  const delta = daysBetween(anchor, date)
  return delta >= 0 && delta % habit.repeatEveryDays === 0
}
"""
          reminder_logic = reminder_logic_anchor + """

export interface HabitReminderTrigger {
  habitId: string
  title: string
  occurrenceDate: string
  unit: number
  targetValue: number
  preferredTime: string
  triggerAt: number
}

const HABIT_REMINDER_OFFSET_MS = 30 * 60_000

function localDateTimeMs(date: string, time: string): number {
  const [year, month, day] = dateParts(date)
  const [hours, minutes] = time.split(':').map(Number)
  return new Date(year, month - 1, day, hours ?? 0, minutes ?? 0, 0, 0).getTime()
}

export function listDueHabitReminders(sinceMs: number, nowMs: number): HabitReminderTrigger[] {
  if (!Number.isFinite(sinceMs) || !Number.isFinite(nowMs) || nowMs < sinceMs) return []

  const sqlite = getSqlite()
  const habits = (
    sqlite
      .prepare(`${HABIT_SELECT} WHERE reminders_enabled = 1 ORDER BY updated_at DESC, created_at DESC`)
      .all() as HabitRow[]
  )
    .map(mapHabit)
    .filter((habit) => habit.remindersEnabled && habit.preferredTimes.length > 0)

  if (habits.length === 0) return []

  const fromDate = localDateKey(new Date(sinceMs))
  const throughDate = addDays(localDateKey(new Date(nowMs)), 1)
  const entryRows = sqlite
    .prepare(`${HABIT_ENTRY_SELECT} WHERE date >= ? AND date <= ?`)
    .all(fromDate, throughDate) as HabitEntryRow[]
  const entries = new Map(entryRows.map((row) => [`${row.habit_id}:${row.date}`, mapEntry(row)]))
  const result: HabitReminderTrigger[] = []

  for (let occurrenceDate = fromDate; occurrenceDate <= throughDate; occurrenceDate = addDays(occurrenceDate, 1)) {
    for (const habit of habits) {
      if (!isHabitScheduledOn(habit, occurrenceDate)) continue

      const entry = entries.get(`${habit.id}:${occurrenceDate}`)
      if (entry?.skipped) continue
      const progress = entry?.value ?? 0

      for (const preferredTime of habit.preferredTimes) {
        if (progress >= preferredTime.unit) continue
        const triggerAt = localDateTimeMs(occurrenceDate, preferredTime.time) - HABIT_REMINDER_OFFSET_MS
        if (triggerAt < sinceMs || triggerAt > nowMs) continue

        result.push({
          habitId: habit.id,
          title: habit.title,
          occurrenceDate,
          unit: preferredTime.unit,
          targetValue: habit.targetValue,
          preferredTime: preferredTime.time,
          triggerAt
        })
      }
    }
  }

  return result.sort(
    (left, right) => left.triggerAt - right.triggerAt || left.title.localeCompare(right.title, 'ru')
  )
}

export function markHabitReminderDelivered(reminder: HabitReminderTrigger): boolean {
  try {
    getSqlite()
      .prepare(
        `INSERT INTO habit_reminder_deliveries (
          id, habit_id, occurrence_date, unit, preferred_time, delivered_at
        ) VALUES (?, ?, ?, ?, ?, ?)`
      )
      .run(
        randomUUID(),
        reminder.habitId,
        reminder.occurrenceDate,
        reminder.unit,
        reminder.preferredTime,
        Date.now()
      )
    return true
  } catch (reason: unknown) {
    if (reason instanceof Error && /UNIQUE constraint failed/.test(reason.message)) return false
    throw reason
  }
}
"""
          text = replace_once(text, reminder_logic_anchor, reminder_logic, 'habit reminder scheduling functions')
          write(path, text)

          # Main-process scheduler mirrors Calendar notifications and focuses the app on click.
          service_path = Path('src/main/services/habit-reminder-scheduler.ts')
          if service_path.exists():
              raise SystemExit('habit reminder scheduler already exists unexpectedly')
          service_path.write_text("""import { Notification, type BrowserWindow } from 'electron'

import {
  listDueHabitReminders,
  markHabitReminderDelivered
} from '../repositories/habits.repository'

const CHECK_INTERVAL_MS = 30_000
const STARTUP_LOOKBACK_MS = 5 * 60_000

export class HabitReminderScheduler {
  private timer: NodeJS.Timeout | null = null
  private lastCheck = Date.now() - STARTUP_LOOKBACK_MS
  private running = false

  constructor(private readonly getWindow: () => BrowserWindow | null) {}

  start(): void {
    if (this.timer) return
    void this.tick()
    this.timer = setInterval(() => void this.tick(), CHECK_INTERVAL_MS)
  }

  stop(): void {
    if (this.timer) clearInterval(this.timer)
    this.timer = null
  }

  private async tick(): Promise<void> {
    if (this.running) return
    this.running = true
    const now = Date.now()

    try {
      const due = listDueHabitReminders(this.lastCheck, now)
      for (const reminder of due) {
        if (!markHabitReminderDelivered(reminder)) continue
        if (!Notification.isSupported()) continue

        const progressLabel =
          reminder.targetValue > 1 ? ` · ${reminder.unit}/${reminder.targetValue}` : ''
        const notification = new Notification({
          title: reminder.title,
          body: `Через 30 минут${progressLabel} · ${reminder.preferredTime}`,
          silent: false,
          timeoutType: 'default'
        })

        notification.on('click', () => {
          const window = this.getWindow()
          if (!window || window.isDestroyed()) return
          if (window.isMinimized()) window.restore()
          window.show()
          window.focus()
        })
        notification.show()
      }
    } catch (reason: unknown) {
      console.error('Habit reminder scheduler failed', reason)
    } finally {
      this.lastCheck = now
      this.running = false
    }
  }
}
""", encoding='utf-8')

          # Start/stop scheduler with the app lifecycle.
          path = 'src/main/index.ts'
          text = read(path)
          text = replace_once(
              text,
              "import { CalendarReminderScheduler } from './services/calendar-reminder-scheduler'\n",
              "import { CalendarReminderScheduler } from './services/calendar-reminder-scheduler'\nimport { HabitReminderScheduler } from './services/habit-reminder-scheduler'\n",
              'main scheduler import',
          )
          text = replace_once(
              text,
              "const calendarReminderScheduler = new CalendarReminderScheduler(() => mainWindow)\n",
              "const calendarReminderScheduler = new CalendarReminderScheduler(() => mainWindow)\nconst habitReminderScheduler = new HabitReminderScheduler(() => mainWindow)\n",
              'main scheduler instance',
          )
          text = replace_once(
              text,
              "  calendarReminderScheduler.stop()\n  clearTrackedPasswordClipboard()",
              "  calendarReminderScheduler.stop()\n  habitReminderScheduler.stop()\n  clearTrackedPasswordClipboard()",
              'main scheduler stop',
          )
          text = replace_once(
              text,
              "    calendarReminderScheduler.start()\n\n    app.on('activate'",
              "    calendarReminderScheduler.start()\n    habitReminderScheduler.start()\n\n    app.on('activate'",
              'main scheduler start',
          )
          write(path, text)

          # Habit editor state and save payload.
          path = 'src/renderer/src/modules/habits/components/HabitDialog.tsx'
          text = read(path)
          text = replace_once(
              text,
              "  const [preferredTimes, setPreferredTimes] = useState<Record<number, string>>({})\n  const [error,",
              "  const [preferredTimes, setPreferredTimes] = useState<Record<number, string>>({})\n  const [remindersEnabled, setRemindersEnabled] = useState(false)\n  const [error,",
              'HabitDialog reminder state',
          )
          text = replace_once(
              text,
              "    setPreferredTimes(preferredTimesToRecord(habit))\n    setError(null)",
              "    setPreferredTimes(preferredTimesToRecord(habit))\n    setRemindersEnabled(habit?.remindersEnabled ?? false)\n    setError(null)",
              'HabitDialog reminder initialize',
          )
          old_tracking = """  function changeTrackingType(value: string): void {
    const next = value as HabitTrackingType
    setTrackingType(next)
    if (next === 'check') {
      setTargetValue('1')
      setUnit('')
      setPreferredTimes((current) => {
        const first = current[1]
        const nextPreferredTimes: Record<number, string> = {}
        if (first) nextPreferredTimes[1] = first
        return nextPreferredTimes
      })
    }
  }
"""
          new_tracking = """  function changeTrackingType(value: string): void {
    const next = value as HabitTrackingType
    setTrackingType(next)
    if (next === 'check') {
      setTargetValue('1')
      setUnit('')
      const first = preferredTimes[1]
      setPreferredTimes(first ? { 1: first } : {})
      if (!first) setRemindersEnabled(false)
    }
  }
"""
          text = replace_once(text, old_tracking, new_tracking, 'HabitDialog tracking type change')
          old_time = """  function changePreferredTime(unitNumber: number, value: string): void {
    setPreferredTimes((current) => {
      const next = { ...current }
      if (value) next[unitNumber] = value
      else delete next[unitNumber]
      return next
    })
  }
"""
          new_time = """  function changePreferredTime(unitNumber: number, value: string): void {
    const next = { ...preferredTimes }
    if (value) next[unitNumber] = value
    else delete next[unitNumber]
    setPreferredTimes(next)
    if (!Object.values(next).some(Boolean)) setRemindersEnabled(false)
  }
"""
          text = replace_once(text, old_time, new_time, 'HabitDialog preferred time change')
          text = replace_once(
              text,
              "      repeatEveryDays: parsedRepeat,\n      preferredTimes: normalizedPreferredTimes\n",
              "      repeatEveryDays: parsedRepeat,\n      preferredTimes: normalizedPreferredTimes,\n      remindersEnabled: normalizedPreferredTimes.length > 0 && remindersEnabled\n",
              'HabitDialog save reminder',
          )
          text = replace_once(
              text,
              "          values={preferredTimes}\n          onChange={changePreferredTime}\n",
              "          values={preferredTimes}\n          remindersEnabled={remindersEnabled}\n          onChange={changePreferredTime}\n          onRemindersChange={setRemindersEnabled}\n",
              'HabitDialog editor reminder props',
          )
          write(path, text)

          # Preferred-time editor: reminder switch only exists when a valid preferred time exists.
          path = 'src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.tsx'
          text = read(path)
          text = replace_once(
              text,
              "import { Tooltip } from '../../../shared/ui/tooltip'\nimport { ChevronLeft, ChevronRight, Clock3 } from 'lucide-react'",
              "import { Tooltip } from '../../../shared/ui/tooltip'\nimport * as Switch from '@radix-ui/react-switch'\nimport { Bell, ChevronLeft, ChevronRight, Clock3 } from 'lucide-react'",
              'preferred times reminder imports',
          )
          text = replace_once(
              text,
              "  values: Record<number, string>\n  onChange: (unit: number, value: string) => void\n",
              "  values: Record<number, string>\n  remindersEnabled?: boolean\n  onChange: (unit: number, value: string) => void\n  onRemindersChange?: (enabled: boolean) => void\n",
              'preferred times reminder props',
          )
          text = replace_once(
              text,
              "  targetValue,\n  values,\n  onChange\n}: HabitPreferredTimesEditorProps)",
              "  targetValue,\n  values,\n  remindersEnabled = false,\n  onChange,\n  onRemindersChange = () => undefined\n}: HabitPreferredTimesEditorProps)",
              'preferred times reminder defaults',
          )
          text = replace_once(
              text,
              "  const units = Array.from({ length: endUnit - startUnit + 1 }, (_, index) => startUnit + index)\n",
              "  const units = Array.from({ length: endUnit - startUnit + 1 }, (_, index) => startUnit + index)\n  const hasPreferredTime = Object.entries(values).some(\n    ([unit, time]) => Number(unit) >= 1 && Number(unit) <= safeTarget && Boolean(time)\n  )\n",
              'preferred times reminder availability',
          )
          ending = """      </div>
    </div>
  )
}
"""
          reminder_ui = """      </div>

      {hasPreferredTime && (
        <div className="mt-4 flex items-center gap-3 rounded-xl border border-violet-400/15 bg-violet-500/[0.055] px-3 py-3">
          <span className="flex size-8 shrink-0 items-center justify-center rounded-lg border border-violet-400/20 bg-violet-500/10 text-violet-200">
            <Bell className="size-4" />
          </span>
          <div className="min-w-0 flex-1">
            <div className="text-sm font-medium text-[var(--app-text)]">Напоминания</div>
            <div className="mt-0.5 text-xs leading-5 text-[var(--app-muted)]">
              За 30 минут до каждого указанного предпочтительного времени.
            </div>
          </div>
          <Switch.Root
            checked={remindersEnabled}
            aria-label="Напоминать о привычке за 30 минут"
            className="relative h-6 w-11 shrink-0 rounded-full border border-[var(--app-border)] bg-[var(--app-control)] outline-none transition-colors data-[state=checked]:border-violet-400/40 data-[state=checked]:bg-violet-500 focus-visible:ring-2 focus-visible:ring-violet-500/30"
            onCheckedChange={onRemindersChange}
          >
            <Switch.Thumb className="block size-4 translate-x-1 rounded-full bg-white shadow-sm transition-transform data-[state=checked]:translate-x-5" />
          </Switch.Root>
        </div>
      )}
    </div>
  )
}
"""
          if text.count(ending) != 1:
              raise SystemExit(f'preferred times end: expected one match, found {text.count(ending)}')
          text = text.replace(ending, reminder_ui, 1)
          write(path, text)

          # Habit details popover: show calendar-like fixed offset when enabled.
          path = 'src/renderer/src/modules/habits/components/HabitTodayRow.tsx'
          text = read(path)
          text = replace_once(
              text,
              "  Check,\n  Clock3,",
              "  Bell,\n  Check,\n  Clock3,",
              'HabitTodayRow Bell import',
          )
          marker = """                {habit.trackingType === 'count' && (
"""
          block = """                {habit.remindersEnabled && habit.preferredTimes.length > 0 && (
                  <div className="flex items-center gap-3 rounded-xl bg-[var(--app-control)] px-3 py-2.5">
                    <span className="flex size-8 shrink-0 items-center justify-center rounded-lg border border-violet-400/20 bg-violet-500/10 text-violet-200">
                      <Bell className="size-4" />
                    </span>
                    <div className="min-w-0 flex-1">
                      <div className="text-[10px] font-medium tracking-[0.12em] text-[var(--app-muted)] uppercase">
                        Напоминания
                      </div>
                      <div className="mt-1.5">
                        <span className="inline-flex h-6 items-center rounded-lg border border-[var(--app-border)] bg-[var(--app-workspace)] px-2 text-[11px] text-[var(--app-text)]">
                          За 30 минут
                        </span>
                      </div>
                    </div>
                  </div>
                )}

""" + marker
          text = replace_once(text, marker, block, 'HabitTodayRow reminder display')
          write(path, text)

          # Validation tests: new field + invariant.
          path = 'src/shared/validation/habits.test.ts'
          text = read(path)
          text = replace_once(
              text,
              "  repeatEveryDays: 1,\n  preferredTimes: []\n",
              "  repeatEveryDays: 1,\n  preferredTimes: [],\n  remindersEnabled: false\n",
              'validation base habit reminder field',
          )
          test_anchor = """  it('rejects legacy fields removed from the habit model', () => {
"""
          reminder_test = """  it('allows reminders only when a preferred time is configured', () => {
    expect(() =>
      createHabitInputSchema.parse({ ...baseHabit, remindersEnabled: true })
    ).toThrow('Чтобы включить напоминания, укажите предпочтительное время')

    expect(
      createHabitInputSchema.parse({
        ...baseHabit,
        preferredTimes: [{ unit: 1, time: '09:00' }],
        remindersEnabled: true
      }).remindersEnabled
    ).toBe(true)
  })

""" + test_anchor
          text = replace_once(text, test_anchor, reminder_test, 'validation reminder test')
          write(path, text)

          # Repository tests: make all direct inputs explicit and cover scheduling/de-dup/progress suppression.
          path = 'src/main/repositories/habits.repository.test.ts'
          text = read(path)
          text = replace_once(
              text,
              "  listHabitsOverview,\n  updateHabit,",
              "  listDueHabitReminders,\n  listHabitsOverview,\n  markHabitReminderDelivered,\n  updateHabit,",
              'repository reminder test imports',
          )
          text = replace_once(
              text,
              "  getSqlite().exec('DELETE FROM habit_entries; DELETE FROM habits; DELETE FROM habit_groups;')",
              "  getSqlite().exec(\n    'DELETE FROM habit_reminder_deliveries; DELETE FROM habit_entries; DELETE FROM habits; DELETE FROM habit_groups;'\n  )",
              'repository reminder test cleanup',
          )
          # Add remindersEnabled: false to every direct create input that has repeatEveryDays followed by preferredTimes.
          pattern = re.compile(r"(?P<indent>\s+)repeatEveryDays: (?P<value>[^,\n]+),\n(?P=indent)preferredTimes:")
          text, count = pattern.subn(
              lambda match: f"{match.group('indent')}repeatEveryDays: {match.group('value')},\n{match.group('indent')}remindersEnabled: false,\n{match.group('indent')}preferredTimes:",
              text,
          )
          if count < 5:
              raise SystemExit(f'repository reminder fields: expected at least 5 inputs, updated {count}')
          # First persistence test should explicitly exercise enabled persistence.
          text = text.replace('      remindersEnabled: false,\n      preferredTimes: [', '      remindersEnabled: true,\n      preferredTimes: [', 1)
          text = replace_once(
              text,
              "      repeatEveryDays: habit.repeatEveryDays,\n      preferredTimes: habit.preferredTimes\n",
              "      repeatEveryDays: habit.repeatEveryDays,\n      preferredTimes: habit.preferredTimes,\n      remindersEnabled: habit.remindersEnabled\n",
              'repository update reminder field',
          )
          text = replace_once(
              text,
              "    expect(habit.preferredTimes).toEqual([",
              "    expect(habit.remindersEnabled).toBe(true)\n    expect(habit.preferredTimes).toEqual([",
              'repository persistence reminder expectation',
          )
          text = replace_once(
              text,
              "    expect(names).not.toContain('archived_on')\n",
              "    expect(names).not.toContain('archived_on')\n    expect(names).toContain('reminders_enabled')\n    expect(\n      getSqlite()\n        .prepare(\"SELECT name FROM sqlite_master WHERE type = 'table' AND name = 'habit_reminder_deliveries'\")\n        .get()\n    ).toBeTruthy()\n",
              'repository migration expectations',
          )
          test_anchor = """  it('reads legacy single preferred_time as the first unit without a migration', () => {
"""
          scheduling_test = """  it('triggers reminders 30 minutes before preferred times and suppresses completed units', () => {
    vi.setSystemTime(new Date(2026, 0, 1, 8, 0, 0))
    const habit = createHabit({
      title: 'Пить воду',
      groupId: null,
      trackingType: 'count',
      targetValue: 3,
      unit: 'стакана',
      repeatEveryDays: 1,
      remindersEnabled: true,
      preferredTimes: [
        { unit: 1, time: '09:00' },
        { unit: 2, time: '15:00' },
        { unit: 3, time: '21:00' }
      ]
    })

    const firstTrigger = new Date(2026, 0, 1, 8, 30, 0).getTime()
    const first = listDueHabitReminders(firstTrigger - 1_000, firstTrigger)
    expect(first).toHaveLength(1)
    expect(first[0]).toMatchObject({
      habitId: habit.id,
      occurrenceDate: '2026-01-01',
      unit: 1,
      targetValue: 3,
      preferredTime: '09:00',
      triggerAt: firstTrigger
    })
    expect(markHabitReminderDelivered(first[0])).toBe(true)
    expect(markHabitReminderDelivered(first[0])).toBe(false)

    upsertHabitEntry({
      habitId: habit.id,
      date: '2026-01-01',
      value: 2,
      skipped: false
    })
    const secondTrigger = new Date(2026, 0, 1, 14, 30, 0).getTime()
    expect(listDueHabitReminders(secondTrigger - 1_000, secondTrigger)).toEqual([])

    const thirdTrigger = new Date(2026, 0, 1, 20, 30, 0).getTime()
    expect(listDueHabitReminders(thirdTrigger - 1_000, thirdTrigger)).toMatchObject([
      { habitId: habit.id, unit: 3, preferredTime: '21:00' }
    ])

    upsertHabitEntry({
      habitId: habit.id,
      date: '2026-01-01',
      value: 0,
      skipped: true
    })
    expect(listDueHabitReminders(thirdTrigger - 1_000, thirdTrigger)).toEqual([])
  })

  it('does not schedule reminders when they are disabled or no preferred time exists', () => {
    vi.setSystemTime(new Date(2026, 0, 1, 8, 0, 0))
    createHabit({
      title: 'Читать',
      groupId: null,
      trackingType: 'check',
      targetValue: 1,
      unit: '',
      repeatEveryDays: 1,
      remindersEnabled: false,
      preferredTimes: [{ unit: 1, time: '09:00' }]
    })
    createHabit({
      title: 'Без времени',
      groupId: null,
      trackingType: 'check',
      targetValue: 1,
      unit: '',
      repeatEveryDays: 1,
      remindersEnabled: false,
      preferredTimes: []
    })

    const trigger = new Date(2026, 0, 1, 8, 30, 0).getTime()
    expect(listDueHabitReminders(trigger - 1_000, trigger)).toEqual([])
  })

""" + test_anchor
          text = replace_once(text, test_anchor, scheduling_test, 'repository reminder scheduling tests')
          write(path, text)

          # Renderer fixture + assertion for calendar-like reminder metadata.
          path = 'src/renderer/src/modules/habits/HabitsPage.test.tsx'
          text = read(path)
          text = replace_once(
              text,
              "    preferredTimes: [{ unit: 1, time: '09:00' }],\n    createdAt:",
              "    preferredTimes: [{ unit: 1, time: '09:00' }],\n    remindersEnabled: true,\n    createdAt:",
              'HabitsPage fixture reminder field',
          )
          text = replace_once(
              text,
              "    expect(popover).toHaveTextContent('09:00')\n",
              "    expect(popover).toHaveTextContent('09:00')\n    expect(popover).toHaveTextContent('За 30 минут')\n",
              'HabitsPage reminder display assertion',
          )
          write(path, text)

          # Component-level reminder UI test. Existing call sites keep defaults for focused time tests.
          path = 'src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.test.tsx'
          text = read(path)
          anchor = """  it('paginates large count targets without rendering every unit at once', async () => {
"""
          ui_test = """  it('shows the fixed reminder switch only when a preferred time exists', async () => {
    const user = userEvent.setup()
    const onRemindersChange = vi.fn()
    const { rerender } = render(
      <HabitPreferredTimesEditor
        trackingType="check"
        targetValue={1}
        values={{}}
        remindersEnabled={false}
        onChange={vi.fn()}
        onRemindersChange={onRemindersChange}
      />
    )

    expect(
      screen.queryByRole('switch', { name: 'Напоминать о привычке за 30 минут' })
    ).not.toBeInTheDocument()

    rerender(
      <HabitPreferredTimesEditor
        trackingType="check"
        targetValue={1}
        values={{ 1: '09:00' }}
        remindersEnabled={false}
        onChange={vi.fn()}
        onRemindersChange={onRemindersChange}
      />
    )

    const reminderSwitch = screen.getByRole('switch', {
      name: 'Напоминать о привычке за 30 минут'
    })
    expect(screen.getByText('За 30 минут до каждого указанного предпочтительного времени.')).toBeInTheDocument()
    await user.click(reminderSwitch)
    expect(onRemindersChange).toHaveBeenCalledWith(true)
  })

""" + anchor
          text = replace_once(text, anchor, ui_test, 'preferred times reminder UI test')
          write(path, text)
          PY

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Generate database migration
        run: npm run db:generate

      - name: Format changed TypeScript
        run: >-
          npx prettier --write
          src/shared/contracts/habits.ts
          src/shared/validation/habits.ts
          src/shared/validation/habits.test.ts
          src/main/database/schema/habits.ts
          src/main/database/schema/index.ts
          src/main/repositories/habits.repository.ts
          src/main/repositories/habits.repository.test.ts
          src/main/services/habit-reminder-scheduler.ts
          src/main/index.ts
          src/renderer/src/modules/habits/components/HabitDialog.tsx
          src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.tsx
          src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.test.tsx
          src/renderer/src/modules/habits/components/HabitTodayRow.tsx
          src/renderer/src/modules/habits/HabitsPage.test.tsx

      - name: Lint changed TypeScript
        run: >-
          npx eslint
          src/shared/contracts/habits.ts
          src/shared/validation/habits.ts
          src/shared/validation/habits.test.ts
          src/main/database/schema/habits.ts
          src/main/database/schema/index.ts
          src/main/repositories/habits.repository.ts
          src/main/repositories/habits.repository.test.ts
          src/main/services/habit-reminder-scheduler.ts
          src/main/index.ts
          src/renderer/src/modules/habits/components/HabitDialog.tsx
          src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.tsx
          src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.test.tsx
          src/renderer/src/modules/habits/components/HabitTodayRow.tsx
          src/renderer/src/modules/habits/HabitsPage.test.tsx

      - name: Rebuild SQLite for tests
        run: npm run native:node

      - name: Run focused habit tests
        run: >-
          npx vitest run
          src/shared/validation/habits.test.ts
          src/main/repositories/habits.repository.test.ts
          src/renderer/src/modules/habits/components/HabitPreferredTimesEditor.test.tsx
          src/renderer/src/modules/habits/HabitsPage.test.tsx

      - name: Node typecheck
        run: npm run typecheck:node

      - name: Web typecheck
        run: npm run typecheck:web

      - name: Check database schema
        run: npm run db:check

      - name: Build production bundle
        run: npm run build:bundle

      - name: Check diff whitespace
        run: git diff --check

      - name: Commit implementation and remove temporary workflow
        shell: bash
        run: |
          rm .github/workflows/apply-habit-reminders.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src drizzle .github/workflows/apply-habit-reminders.yml
          git commit -m "Добавить напоминания для привычек"
          git push origin HEAD:agent/habit-reminders
