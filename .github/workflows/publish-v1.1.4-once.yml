name: Publish MyMind 1.1.4

on:
  push:
    branches:
      - agent/publish-v1.1.4

permissions:
  contents: write

concurrency:
  group: publish-mymind-1-1-4
  cancel-in-progress: false

jobs:
  verify-main:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Verify release versions
        shell: bash
        run: |
          set -euo pipefail
          desktop_version="$(node -p "require('./apps/desktop/package.json').version")"
          mobile_version="$(node -p "require('./apps/mobile/package.json').version")"
          app_version="$(node -p "require('./apps/mobile/app.json').expo.version")"
          version_code="$(node -p "require('./apps/mobile/app.json').expo.android.versionCode")"

          test "$desktop_version" = "1.1.4"
          test "$mobile_version" = "1.1.4"
          test "$app_version" = "1.1.4"
          test "$version_code" = "10104"

      - run: npm run typecheck
      - run: npm run native:node
      - run: npm run test:run
      - run: npm run test:mobile
      - run: npm run db:check

  build-windows:
    needs: verify-main
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build:win

      - name: Verify Windows release files
        shell: pwsh
        run: |
          $files = @(
            "apps/desktop/dist/latest.yml",
            "apps/desktop/dist/mymind-next-1.1.4-setup.exe",
            "apps/desktop/dist/mymind-next-1.1.4-setup.exe.blockmap"
          )
          foreach ($file in $files) {
            if (-not (Test-Path $file)) {
              throw "Missing Windows release artifact: $file"
            }
          }

      - uses: actions/upload-artifact@v4
        with:
          name: windows-release-1.1.4
          if-no-files-found: error
          path: |
            apps/desktop/dist/latest.yml
            apps/desktop/dist/mymind-next-1.1.4-setup.exe
            apps/desktop/dist/mymind-next-1.1.4-setup.exe.blockmap

  build-android:
    needs: verify-main
    runs-on: windows-latest
    timeout-minutes: 50
    env:
      MYMIND_ANDROID_KEYSTORE_PASSWORD: ${{ secrets.MYMIND_ANDROID_KEYSTORE_PASSWORD }}
      MYMIND_ANDROID_KEY_ALIAS: ${{ secrets.MYMIND_ANDROID_KEY_ALIAS }}
      MYMIND_ANDROID_KEY_PASSWORD: ${{ secrets.MYMIND_ANDROID_KEY_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - run: npm ci

      - name: Prepare Android release keystore
        shell: pwsh
        env:
          MYMIND_ANDROID_KEYSTORE_BASE64: ${{ secrets.MYMIND_ANDROID_KEYSTORE_BASE64 }}
        run: |
          $required = @{
            MYMIND_ANDROID_KEYSTORE_BASE64 = $env:MYMIND_ANDROID_KEYSTORE_BASE64
            MYMIND_ANDROID_KEYSTORE_PASSWORD = $env:MYMIND_ANDROID_KEYSTORE_PASSWORD
            MYMIND_ANDROID_KEY_ALIAS = $env:MYMIND_ANDROID_KEY_ALIAS
            MYMIND_ANDROID_KEY_PASSWORD = $env:MYMIND_ANDROID_KEY_PASSWORD
          }

          foreach ($entry in $required.GetEnumerator()) {
            if ([string]::IsNullOrWhiteSpace($entry.Value)) {
              throw "Missing required GitHub secret: $($entry.Key)"
            }
          }

          $keystorePath = Join-Path $env:RUNNER_TEMP "mymind-android-release.jks"
          [IO.File]::WriteAllBytes(
            $keystorePath,
            [Convert]::FromBase64String($env:MYMIND_ANDROID_KEYSTORE_BASE64)
          )

          $expectedFingerprint = "75fbc676fa8e8b8ed89824fc12f95e03a33fc057b33f034edab85f9a9d890508"
          $keytoolOutput = keytool -list -v `
            -keystore $keystorePath `
            -storepass $env:MYMIND_ANDROID_KEYSTORE_PASSWORD `
            -alias $env:MYMIND_ANDROID_KEY_ALIAS | Out-String

          if ($LASTEXITCODE -ne 0) {
            throw "Android release keystore could not be opened"
          }

          $fingerprintMatch = [regex]::Match($keytoolOutput, "SHA256:\s*([0-9A-Fa-f:]+)")
          if (-not $fingerprintMatch.Success) {
            throw "Could not read Android release certificate SHA-256 fingerprint"
          }

          $actualFingerprint = $fingerprintMatch.Groups[1].Value.Replace(":", "").ToLowerInvariant()
          if ($actualFingerprint -ne $expectedFingerprint) {
            throw "Android release signing key fingerprint does not match the permanent MyMind key"
          }

          "MYMIND_ANDROID_KEYSTORE_PATH=$keystorePath" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Build signed Android release APK
        run: npm run build:android:release --workspace=mymind-mobile

      - name: Verify and stage signed Android APK
        shell: pwsh
        run: |
          $source = "apps/mobile/android/app/build/outputs/apk/release/app-release.apk"
          $targetDirectory = "apps/mobile/release"
          $target = "$targetDirectory/mymind-mobile-1.1.4.apk"

          if (-not (Test-Path $source)) {
            throw "Missing Android release APK: $source"
          }

          New-Item -ItemType Directory -Force -Path $targetDirectory | Out-Null
          Copy-Item $source $target

          $apksigner = Get-ChildItem "$env:ANDROID_HOME\build-tools\*\apksigner.bat" |
            Sort-Object { [version]$_.Directory.Name } -Descending |
            Select-Object -First 1

          if (-not $apksigner) {
            throw "Android apksigner was not found"
          }

          & $apksigner.FullName verify --verbose --print-certs $target
          if ($LASTEXITCODE -ne 0) {
            throw "APK signature verification failed"
          }

          Write-Host "APK_SHA256=$((Get-FileHash $target -Algorithm SHA256).Hash.ToLowerInvariant())"

      - uses: actions/upload-artifact@v4
        with:
          name: android-release-1.1.4
          if-no-files-found: error
          path: apps/mobile/release/mymind-mobile-1.1.4.apk

  publish:
    needs:
      - build-windows
      - build-android
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/download-artifact@v4
        with:
          name: windows-release-1.1.4
          path: release/windows

      - uses: actions/download-artifact@v4
        with:
          name: android-release-1.1.4
          path: release/android

      - name: Verify release payload
        shell: bash
        run: |
          set -euo pipefail
          test -s release/windows/latest.yml
          test -s release/windows/mymind-next-1.1.4-setup.exe
          test -s release/windows/mymind-next-1.1.4-setup.exe.blockmap
          test -s release/android/mymind-mobile-1.1.4.apk
          grep -q "version: 1.1.4" release/windows/latest.yml

      - name: Publish GitHub Release v1.1.4
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          target_sha="$(git rev-parse HEAD)"

          if gh release view v1.1.4 --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release v1.1.4 already exists; uploading assets with --clobber."
          else
            gh release create v1.1.4               --repo "$GITHUB_REPOSITORY"               --target "$target_sha"               --title "1.1.4"               --notes "MyMind 1.1.4: фирменная Android-иконка и готовые обновления APK из приложения."
          fi

          gh release upload v1.1.4             --repo "$GITHUB_REPOSITORY"             --clobber             release/windows/latest.yml             release/windows/mymind-next-1.1.4-setup.exe             release/windows/mymind-next-1.1.4-setup.exe.blockmap             release/android/mymind-mobile-1.1.4.apk

          gh release edit v1.1.4             --repo "$GITHUB_REPOSITORY"             --draft=false             --prerelease=false             --latest
