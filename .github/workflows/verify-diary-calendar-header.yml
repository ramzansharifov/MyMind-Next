name: Format diary calendar header

on:
  push:
    branches:
      - agent/diary-calendar-header-month
    paths:
      - .github/workflows/verify-diary-calendar-header.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/diary-calendar-header-month
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx prettier --write src/renderer/src/modules/diary/DiaryPage.tsx src/renderer/src/modules/diary/components/DiaryCalendar.tsx
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/renderer/src/modules/diary/DiaryPage.tsx src/renderer/src/modules/diary/components/DiaryCalendar.tsx
          git diff --cached --quiet || git commit -m "Format diary calendar header"
          git push origin HEAD:agent/diary-calendar-header-month
