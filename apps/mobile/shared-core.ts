import { createCoreContext } from '@mymind/core'
import { designTokens } from '@mymind/design'

export const mobileCoreContext = createCoreContext('mobile')
export const mobileDesignTokens = designTokens
