# Mobile implementation and continuation

Baseline: `038f051fab36bdd988f172802dac37773613e7b1` (PR #279), verified against GitHub main on 2026-09-06 through the GitHub connector. Native Git HTTPS fetch is unavailable in this environment. This is a staged implementation, **not full desktop parity**.

## Audit map

For every module, the source chain is `apps/desktop/src/renderer/src/modules/<module>` → `api/*-client.ts` → `src/preload/index.ts` → `src/main/ipc/register-<module>-ipc.ts` → `src/main/repositories/<module>.repository.ts` → `src/main/database/schema/<module>.ts` and Drizzle migrations. Shared input schemas are the validation boundary; renderer state alone is not persistence.

| Module    | Desktop behavior and constraints                                                                                                                                                                                      | Shared / mobile work                                                                   | Remaining parity work                                                                                                                                                                                            |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Home      | Registry of 15 modules, module resources, real dashboard data                                                                                                                                                         | Native dashboard from local repositories                                               | Full configurable desktop widget equivalents, cross-module resource links                                                                                                                                        |
| Study     | Nested folders/materials, versioned block documents, read/edit/focus, resource navigation, code workspace, links, assets, PDF, duplicate, reorder, autosave, deletion barriers                                        | Audited contracts, repository and lifecycle architecture                               | Native document renderer/editor, asset adapter, code workspace, PDF, atomic document saves, transition flush, deletion coordination; must preserve unsupported blocks                                            |
| Boards    | Folder trees, tldraw snapshots, gestures, autosave, system roots for Study/Notes, protected linked boards, select/edit/pan/zoom                                                                                       | Audited board contract and repository                                                  | Native canvas and snapshot adapter; never silently replace tldraw documents with an incompatible shape model                                                                                                     |
| Notes     | Groups/icons, recents, title/content search, move, block editor, voice recording, attachments, embedded boards, autosave                                                                                              | Audited document format and repository                                                 | Fullscreen native block editor, group CRUD, SQLite documents, safe autosave, file/voice adapters, linked-board cleanup                                                                                           |
| Tasks     | Active/completed, priority, due date/time, group CRUD/colors/icons, quick add, search, group/status filters; deleting group SET NULL; completion timestamp preserved on edits                                         | Contracts, validation and existing SQL repository extracted for both platforms         | See implementation and verification section below                                                                                                                                                                |
| Habits    | Check/count, weekdays overriding interval, preferred times by unit, auto-enabled reminders, day completion/skip/reset, groups, date/group reports, streaks; skip preserves streak; today incomplete does not break it | Contracts/validation/repository shared; pure date/schedule/streak calculations in core | Native notification delivery and background rescheduling, report charts                                                                                                                                          |
| Workouts  | Journal, exercise library, programs, sessions/sets, progress/body metrics/photos, reports, default exercises and muscle map                                                                                           | Source tabs, contracts, repository and asset service audited                           | Entire native workflow, SQLite adapter, camera/file progress photos, shared volume/report calculations                                                                                                           |
| Nutrition | Today/diary/goal/progress, food and recipe libraries, ingredients, meal logs, water, global targets, imports and reports; snapshots preserve history                                                                  | Source tabs, contracts and repository audited                                          | Entire native workflow, shared calculations, transactional meal imports, recipe relations                                                                                                                        |
| Calendar  | One-time/annual events, occurrence notes/hiding, start date elapsed time, offsets, delivered/unread reminders                                                                                                         | Shared contract/validation and exact repository recurrence behavior                    | Native reminder scheduling/delivery, richer month navigation                                                                                                                                                     |
| Diary     | Multiple diaries, appearance/icon, day pages, mood, timestamped entries, histories, mood reports; empty days pruned; cascade deletes                                                                                  | Shared contracts, validation, repository and reports                                   | Notebook visual treatment adapted to native surfaces                                                                                                                                                             |
| Movies    | Movie/series/cartoon/animated series, watchlist/watched, favorites, rating, genres/actors, all metadata, batch add, search/filter/sort, web search                                                                    | Shared contracts/validation/repository                                                 | Batch import UI and richer visual statistics                                                                                                                                                                     |
| Music     | Track/album/EP/single catalog, artists/genres, status, favorites/rating, metadata, playlists/covers, membership, web search, batch add                                                                                | Shared contracts/validation/repository                                                 | Batch import UI; verify current desktop catalog semantics before introducing a separate local audio player                                                                                                       |
| Finance   | Accounts, manual FX, ledger/entries, income/expense/transfers, tags, limits, templates, reports, history cleanup; minor units; history-protected accounts/tags, transactional transfer adjustments                    | Full contract inventory, architecture and services identified                          | Port finance-money/conversion/periods and ledger services; native accounts/forms/reports. Do not substitute stored balances or float arithmetic. Current desktop has limits/templates, not a generic goals model |
| Passwords | scrypt N=32768/r=8/p=1, wrapped DEK, AES-GCM, encrypted credentials, lock/unlock/reveal/copy/security review, clipboard TTL and shutdown lock                                                                         | Encryption repository and platform boundary audited                                    | SecureStore/Keychain/Keystore crypto adapter, KDF parity, lifecycle lock, clipboard owner-aware clearing, secure native UI. No plaintext fallback or dummy vault                                                 |
| Settings  | system/light/dark, five accents, local appearance persistence; desktop-only window/storage options                                                                                                                    | Shared appearance contract/validation and semantic tokens                              | Platform-applicable storage/export preferences                                                                                                                                                                   |

## Architecture decisions

- Native React Native UI only. No renderer imports, DOM JSX, WebView-based desktop app, or mock records.
- `@mymind/contracts`: existing domain types and platform-neutral ports. Desktop source paths re-export shared definitions to keep IPC consumers stable.
- `@mymind/core`: existing Zod validation and pure rules. No SQL driver, Expo, Electron or application imports.
- `@mymind/persistence`: shared SQL repository factories, row mapping and atomic transactions. Added separately to avoid placing persistence implementation in domain core. The platform passes a database/UUID/clock runtime; no global mutable driver switch.
- Expo SQLite local database, foreign keys, WAL, versioned transactional migrations. Production database is never reset during initialization. Settings use the same database. No cloud backend.
- Porting a repository does not claim that every platform service or UI feature is complete. Native notifications, document assets and vault security require distinct adapters and runtime checks.

## Verification and continuation

This section is updated with exact commands/results before delivery. Full parity and merge remain gated on all modules, desktop regressions, Android export/runtime and successful CI. The existing GitHub workflow is manual (`workflow_dispatch`) and currently does not build/install Windows packages or export Android.

Next implement native Notes/Study document persistence and a lossless block editor with guarded navigation. Reuse the desktop autosave queue/deletion barrier after extracting its platform-neutral dependencies; test failed save, switch during save, delete during save, delete rollback, background/foreground and restart. Then implement boards, workouts/nutrition, finance and vault. Each must have working persistence and its own integration checks before being listed as implemented.
